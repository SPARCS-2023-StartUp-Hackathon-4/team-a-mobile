import 'package:flutter/material.dart';

class CommunityFailureLayout extends StatelessWidget {
  const CommunityFailureLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
