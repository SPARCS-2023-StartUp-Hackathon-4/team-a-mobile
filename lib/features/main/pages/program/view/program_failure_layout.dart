import 'package:flutter/material.dart';

class ProgramFailureLayout extends StatelessWidget {
  const ProgramFailureLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
