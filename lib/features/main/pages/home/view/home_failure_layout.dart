import 'package:flutter/material.dart';

class HomeFailureLayout extends StatelessWidget {
  const HomeFailureLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
